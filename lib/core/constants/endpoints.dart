class Endpoints {
  // Base URL
  static const String baseUrl = 'https://cosmarket.cosmictech.vn/api/gold';

  // Auth endpoints
  static const String feedToken = '$baseUrl/feed/token';

  // Gold endpoints
  static const String goldPrice = '$baseUrl/feed';
}
